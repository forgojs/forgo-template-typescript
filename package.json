{
  "name": "your-forgo-app-name",
  "private": true,
  "version": "0.0.1",
  "description": "A template you can use to get started with Forgo and TypeScript",
  "scripts": {
    "start": "npx webpack serve",
    "build": "tsc --noEmit && npx webpack --mode production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "forgo": "^1.1.1"
  },
  "devDependencies": {
    "esbuild-loader": "^2.10.0",
    "html-webpack-plugin": "^5.3.1",
    "typescript": "^4.2.3",
    "webpack-cli": "^4.5.0",
    "webpack-dev-server": "^3.11.2"
  }
}
