{
  "name": "your-forgo-app-name",
  "private": true,
  "version": "0.0.1",
  "description": "A template you can use to get started with Forgo and TypeScript",
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider npx webpack serve --mode development",
    "build": "tsc --noEmit && NODE_OPTIONS=--openssl-legacy-provider  npx webpack --mode production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "forgo": "^3.1.0"
  },
  "devDependencies": {
    "esbuild-loader": "^2.18.0",
    "html-webpack-plugin": "^5.5.0",
    "typescript": "^4.5.5",
    "webpack-cli": "^4.9.2",
    "webpack-dev-server": "^4.7.4"
  }
}
